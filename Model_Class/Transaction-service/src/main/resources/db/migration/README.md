Place the approved TRANSACTION_SCHEMA DDL here when Flyway migrations are introduced.
The current project keeps ddl-auto=validate so the application validates against the existing Oracle schema rather than modifying it.
