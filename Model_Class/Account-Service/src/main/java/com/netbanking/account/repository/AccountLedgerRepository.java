package com.netbanking.account.repository; import com.netbanking.account.entity.AccountLedger; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface AccountLedgerRepository extends JpaRepository<AccountLedger,Long>{ boolean existsByEntryReference(String entryReference); List<AccountLedger> findByAccountIdOrderByCreatedAtDesc(Long accountId); }
